{
  "name": "co-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-no-secrets.js && node --check src/main.js && node --check sw.js && node --check scripts/refresh-vercel-auth.js && node --check api/samsung-electronics-quote.js && node --check api/daum-dictionary.js",
    "build": "npm run check"
  }
}

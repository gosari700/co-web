{
  "name": "co-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-no-secrets.js && node --check src/main.js && node --check sw.js",
    "build": "npm run check"
  }
}
